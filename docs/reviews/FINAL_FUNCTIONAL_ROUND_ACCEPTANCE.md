# Final Functional Round — Acceptance

Scope: five stakeholder items. No change to commission, settlement, refund, order-total, shipping, tax, discount or ledger-creation code.

| ID | Requirement | Reproduced Root Cause | Implementation | Tests | Browser Evidence | Final Status |
|----|-------------|-----------------------|----------------|-------|------------------|--------------|
| 1 | Syndicate vendor report shows vendor profit (ledger net earnings), in screen and PDF, ar/en | The report showed sales/commission/refunds/net only as small KPI cells; no distinct "vendor profit" figure. Canonical `finance.net_earnings` (sale − commission − refunds, from `vendor_ledger_entries`) already existed. | `ProfitCard` in `Vendor360.jsx` (syndicate mode) and a highlighted profit block in `reports/syndicate-vendor.blade.php`, both reading the existing service data. New ar/en labels. No new calculation. Contrast fix on the hint text. | `SyndicateFinalRoundTest`: profit = 850 from ledger; commission deducted; refund lowers it; 400 settlement does not; later category-commission edit has no effect; other vendor's ledger does not leak; PDF data/view contain the label and figure; other syndicate gets 404 on analytics and PDF. | `reports/vendor-profit-ar-1024.png`, `reports/vendor-ar-{375,768,1024,1440}.png`, `reports/final/vendor-report-ar.pdf` (2 pages, rasterized at 150 DPI and inspected page by page; net 3,647.72 matches the screen). axe: 0 critical/serious. | FIXED_AND_VERIFIED |
| 2 | Remove black map background, keep 14-governorate behaviour | Container was hard-coded `bg-black`, with fills and hover/selected colours tuned to black (computed `rgb(0,0,0)` in light and dark). | `DashboardVendorMap.jsx`: container `border-border bg-muted/30`; fills via `color-mix` of `--color-primary`/`--color-muted`; selected/focus use theme tokens. Geometry untouched. | Build passes; no map-logic change. | `map/before-*` vs `map/after-*`; final interaction pass in `map/final/` at 375/768/1024/1440, light and dark: 14 paths, Damascus hover and keyboard focus open the tooltip (Arabic label, count 1 = DB), tooltip inside its card and viewport, Tab moves to the next governorate, Enter/Space do nothing on the syndicate map (admin-only drilldown, by design), token background (never black), zero page overflow, zero console errors. | FIXED_AND_VERIFIED |
| 3 | Notifications reach the intended users only | Reproduced: a marketing notice reached all seven roles (admin, customers, vendors, syndicate, employee) and was also broadcast on the public channel `notifications.public`. | `AdminNotificationSent` broadcasts only on per-recipient private channels; public-channel listeners removed from `echo.js`/`notifications.js`. The marketing audience is unchanged from the canonical rule (every user who has not opted out of the `marketing` category) - see Decisions. | `NotificationTargetingTest` (8): marketing reaches every opted-in user and no guest, no public channel, opt-out removes customers, syndicate and vendors alike, order-notice recipients, targeted send to syndicate/vendor/employee/customer, non-admin 403, mark-one and mark-all isolation, guest 401. Plus existing privacy/preferences tests. | `notifications/notification-targeting-test-run.txt` (test run output). No browser screenshot was captured for this item; the proof is at API/DB level (feature tests exercising the real endpoints). | FIXED_AND_VERIFIED |
| 4 | Syndicate product click opens Product Detail | Not literally reproduced as "goes to Reports": the products table rows had no link at all and the Vendor360 product link was `href='#'`. No detail route existed. | New API `GET /api/syndicate/products/{id}` (domain-scoped, 404 otherwise), web route `syndicate.products.show`, page `Syndicate/Products/Show.jsx`, row link and "view details" action in the table/mobile card, Vendor360 link fixed. Public-safe fields only. | Route names/components; 200 in scope; 404 out-of-scope/missing; 403 for other roles; 401 guest; no rejection reason, commission, vendor email/phone. | `syndicate-products/before-…`, `after-…`, `product-detail-ar-1440.png`, `product-ar-*` and `products-ar-*` at 375/768/1024/1440; click lands on `/syndicate/products/10`. axe: 0 critical/serious. | FIXED_AND_VERIFIED |
| 5 | Syndicate orders have a detail view | No order detail route/page; list rows were not links; the list API also returned the customer email. | API `GET /api/syndicate/orders/{id}` (domain-scoped), route `syndicate.orders.show`, page `Syndicate/Orders/Show.jsx`, "View details" in table and mobile card, breadcrumb and back link. Customer name and governorate only; no phone, alternate phone, email, street, notes or cancellation notes. Mixed-domain orders show only in-domain lines and hide order-level totals. The list API now selects an explicit safe column list (it previously returned the whole order row, including phone, alternate phone, street and notes) and no longer returns the email. | Detail has items and totals; no private data anywhere in the payload; mixed order hides other-domain lines and totals; out-of-scope 404; other roles 403; guest 401. | `syndicate-orders/orders-list-ar-1440.png`, `order-detail-ar-1440.png`, `orders-ar-*`/`order-ar-*` at 375/768/1024/1440; click lands on `/syndicate/orders/6`. axe: 0 critical/serious. | FIXED_AND_VERIFIED |

## Decisions recorded
- **Notification audience (resolved by evidence, not assumption):** `docs/architecture/IMPLEMENTATION_DECISIONS.md` D18 and the `notification_preferences` copy ("New products & offers", "Shared with everyone on Vetora") define marketing notices as addressed to every user, opt-out via the `marketing` preference. An earlier draft of this round hard-coded "customers only"; that has been reverted. What changed for good is the transport: delivery is per-recipient over private channels only, never the public `notifications.public` channel that any client could subscribe to.
- "Vendor profit" = `net_earnings` (sale - commission - refunds), with settlements shown separately as settled/outstanding.
- The Syndicate order list and order detail show the customer's name only (plus governorate on detail); no phone, alternate phone, email, street, notes or cancellation notes.

## Open items (not hidden)
- **Vendor 360 syndicate drill-down still shows customer PII.** The pre-existing vendor "orders" and "overview" tabs (`VendorAnalyticsService::presentOrder`, documented in code as a deliberate operational drill-down) return the customer's email, phone and full shipping address to syndicate viewers. This is outside the five tasks and was left unchanged; it is inconsistent with the minimal exposure now applied to the order detail page. It needs a business decision on whether syndicates have a basis for that data. If not, restrict `customer` and `shipping_address` in `presentOrder` when `$domain !== null`.
- Seed order 6 shows subtotal 980 + shipping 15 against a stored total of 980; seed data, unaffected by this round.
- 13 pre-existing hardcoded-text scan findings remain (none introduced here); the scan exits 0.
- PDF: the header shows "غير متاح" (not available) next to the title for an all-time range, and the logo asset has a baked-in checkerboard background. Both are pre-existing and were left alone.
- Out-of-scope product/order URLs are proven by API tests (404), not by browser screenshots.

## Gates (fresh, closing run)
- `php artisan test --compact`: 508 passed (12326 assertions)
- `vendor/bin/pint --test`: pass
- `npm run build`: success (client and SSR)
- `git diff --check`: clean
- `node scripts/scan-hardcoded-text.mjs`: exit 0, 13 pre-existing findings, none in files added by this round
- `composer audit`: no advisories; `npm audit`: 0 vulnerabilities
